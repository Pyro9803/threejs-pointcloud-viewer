node_modules/
dist/
# Uncomment the line below to exclude tileset from git (local dev only).
# If deploying to Vercel, the tileset must be committed so Vercel can serve it.
# public/tileset/*
# !public/tileset/.gitkeep
.DS_Store
